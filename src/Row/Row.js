import React from 'react';
import { View, TouchableOpacity } from 'react-native';
import {} from 'prop-types';

const propTypes = {};
const defaultProps = {};

const Row = props => <View style={styles.left} />;

Row.propTypes = propTypes;
Row.defaultProps = defaultProps;

export default Row;
